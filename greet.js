const getInput = require("./get-input.js");

//use console.log to print out a greeting to user.

// 1. get the user's name and save it in a variable.
const name = getInput();
// 2. print out what's in that variable.
console.log("Hello, " + name + "!");
